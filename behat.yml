default:
  suites:
    default:
      contexts:
        - App\Tests\Behat\DatabaseContext:
            @doctrine: '@doctrine'
        - FeatureContext:
            kernel: '@kernel'

  extensions:
    Behat\Symfony2Extension:
      kernel:
        bootstrap: features/bootstrap/bootstrap.php
        class: App\Kernel
